# Secrets and local configuration — never commit these.
.env
.env.*
!.env.example
*.pem
*.key

# Compiled binaries
/sqzd
*.exe
*.dll
*.so
*.dylib

# Go test and coverage output
*.test
*.out
coverage.*

# Editor / OS noise
.DS_Store
.idea/
.vscode/
*.swp

# Stray screenshot artifacts from CLI flag typos (e.g. a file literally
# named `--full-page`).
--*
.claude/
